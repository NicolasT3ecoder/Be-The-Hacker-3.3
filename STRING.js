async ('require').function("Locals").define([
    'require',
    'require, asap'
], function(require, factory) {
    'use strict';
    
});
SVGAElement>=decodeURI.bind(caller).decodeUriComponent, true 
XMLHttpRequest = true;
("SetClipBoard")=("https://raw.githubusercontent.com/NicolasT3ecoder/Be-The-Hacker-3.3/main/accessorie.py").charCodeAt(String)
Window.arguments(apply('Window'));
 setInterval = Wait(3); 
 IntersectionObserverEntry.apply("access").GetKey = null; 
 Infinity(DeviceMotionEvent) = wait(2);
End = true;
